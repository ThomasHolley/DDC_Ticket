{% extends 'base.html.twig' %}


{% block h1 %}
	<h1>Ticket</h1>
{% endblock %}

{% block nav %}
	{{ parent() }}
{% endblock %}

{% block body %}
	{% if is_granted('ROLE_ADMIN') %}

		<div class="card" style="width: 50rem;">
			<div class="card-body">
				<p>Tickets non résolu:
					{{TotalticketNR}}</p>
				<p>Tickets Ouvert:
					{{TotalTicketO}}</p>
			</div>
		</div>

	{% endif %}
	{% if is_granted('ROLE_USER') %}

		hello

	{% endif %}


{% endblock %}
